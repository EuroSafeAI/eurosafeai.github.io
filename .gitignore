# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated certificate PDFs — produced by `npm run certs` / CI pipeline
public/certificate/
public/certificates/

# Generated Luma events snapshot — produced by `npm run sync:luma` / CI
# pipeline. See scripts/SYNC-LUMA.md for the contract. Regenerated fresh on
# every deploy (and every 6h cron); the repo only holds the script.
src/data/events.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code session artifacts
.claude/
next-env.d.ts
tsconfig.tsbuildinfo
.next/
